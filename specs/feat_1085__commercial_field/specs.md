We want to update listing schema to add new fields to support Commercial listings:

- units: Number of units available in the commercial property
- revenue: Estimated revenue generated by the commercial property

# Domain

Add the new fields in the domain layer and the DB layer.
Update the

# DTO

The new fields should be added into the Listing DTOs

