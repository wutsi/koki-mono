package com.wutsi.koki.platform.translation

import com.wutsi.koki.platform.translation.ai.AITranslationService
import com.wutsi.koki.platform.translation.aws.AWSTranslationService

class TranslationServiceBuilder(
) {
    fun build(configs: Map<String, String>): TranslationService {
        TODO()
    }
}
