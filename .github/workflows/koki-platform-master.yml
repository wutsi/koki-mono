name: koki-platform

on:
    push:
        branches:
            - master
        paths:
            - 'modules/koki-platform/**'
            - '.github/workflows/koki-platform-master.yml'

jobs:
    build:

        runs-on: ubuntu-latest

        steps:
            -   uses: actions/checkout@v5
            -   name: JDK
                uses: actions/setup-java@v5
                with:
                    distribution: 'zulu'
                    java-version: 17
            -   name: Set up Maven
                uses: stCarolas/setup-maven@v4.5
                with:
                    maven-version: 3.8.2
            -   name: Build
                env:
                    GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
                    GEMINI_API_KEY: ${{secrets.GEMINI_API_KEY_TEST}}
                    STRIPE_API_KEY: ${{secrets.STRIPE_API_KEY_TEST}}
                    DEEPSEEK_API_KEY: ${{secrets.DEEPSEEK_API_KEY_TEST}}
                    BITLY_API_KEY: ${{secrets.BITLY_API_KEY_TEST}}
                run: |
                    mvn -s settings.xml --non-recursive install
                    cd modules/koki-platform
                    git config user.email "actions@github.com"
                    git config user.name "GitHub Actions"
                    git pull
                    mvn -s ../../settings.xml -q -B -X release:prepare release:perform

            -   name: Upload JaCoCo Report
                uses: actions/upload-artifact@v5
                with:
                    name: jacoco-report
                    path: modules/koki-platform/target/site/jacoco/**/*

            -   name: Generate JaCoCo Badge
                uses: cicirello/jacoco-badge-generator@v2
                with:
                    generate-branches-badge: false
                    jacoco-csv-file: modules/koki-platform/target/site/jacoco/jacoco.csv
                    coverage-badge-filename: koki-platform-jacoco.svg

            -   name: Commit the badge (if it changed)
                continue-on-error: true
                run: |
                    if [[ `git status --porcelain` ]]; then
                      git config user.email "actions@github.com"
                      git config user.name "GitHub Actions"
                      git add -A
                      git commit -m "Autogenerated JaCoCo coverage badge"
                      git push
                    fi

